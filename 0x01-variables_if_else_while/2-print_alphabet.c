#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - alphabet in lowercase
 * Return: Always 0
 */
int main(void)
{
	char lowercase;

	lowercase = 'a';
	while
		(lowercase <= 'z')
		{
		putchar(lowercase);
		lowercase++;
		}
	putchar('\n');
	return (0);
}
