#include <stdio.h>
#include <stdlib.h>
/**
 * main - lower and uppercase
 * Return: Always 0
 */
int main(void)
{
	char q;

	char r;

	q = "a";
	r = "a";
	while
		(q <= "z")
		{
			putchar(q);
			q++;
		}
	while
		(r <= "Z")
		{
			putchar(r);
			r++;
		}
	putchar('\n');
	return (0i);
}
